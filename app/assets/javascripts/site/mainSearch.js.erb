$(document).ready(function() {
  $('#procurer_name').autocomplete({
    source: $('#procurer_name').data('autocomplete-source'),
    minLength: 2
  });

  $('#supplier_name').autocomplete({
    source: $('#supplier_name').data('autocomplete-source'),
    minLength: 2
  });

  $('#procurer_search_name').autocomplete({
    source: $('#procurer_search_name').data('autocomplete-source'),
    minLength: 2
  });

  $('#supplier_search_name').autocomplete({
    source: $('#supplier_search_name').data('autocomplete-source'),
    minLength: 2
  });

});
