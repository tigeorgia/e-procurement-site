$(document).ready(function() {
 $('#supplier_name2').autocomplete({
    source: $('.proc_search').data('autocomplete-source'),
    minLength: 2
  });

  $('#supplier_name').autocomplete({
    source: ['your', 'face', 'fff']
  });

  $('#supplier_name').text = "500";
});
