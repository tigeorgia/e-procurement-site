<%if not Rails.application.config.live_deploy%>
<%= button_to(t("Download Data"), {:controller => :analysis, :action => "download_tender_type_amount_data", :format => "csv", :year => @year},:class => "submit-button") %>
<%end%>

<div id="amountGraph" style="min-width: 400px; height: 400px; margin: 0 auto"></div>
