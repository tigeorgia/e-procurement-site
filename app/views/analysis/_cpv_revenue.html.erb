<%if not Rails.application.config.live_deploy%><%= button_to(t("Download Data"), {:controller => :analysis, :action => "download_cpv_data", :format => "csv", :year => @year},:class => "submit-button") %>
<%end%>
<div id="cpvChart"></div>
