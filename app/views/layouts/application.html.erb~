<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
		<script type="text/javascript">
			I18n.defaultLocale = "<%= I18n.default_locale %>";
			I18n.locale = "<%= I18n.locale %>";
		</script>

    <!-- Javascripts
    ================================================== --> 

		<% if Rails.env.production? %>
			<!-- insert google analytics for production site here -->
		<% elsif Rails.env.staging? %>
			<!-- insert google analytics for staging site here -->
		<% end %>

    <title><%= content_for?(:title) ? "#{yield(:title)} > #{t('app.common.app_name')}" :
				t('app.common.app_name') %></title>
    <%= csrf_meta_tags %>


    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

		<!--[if IE 6]><%= stylesheet_link_tag 'ie6' %><![endif]-->
	  <!--[if IE 7]><%= stylesheet_link_tag 'ie7' %><![endif]-->

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag '/favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>
   <div id="wrap">
    <div class="main-menu" style="height: 70px">
      <div class="container" style="height: 100%">
        <div class="heading" style="height: 100%">
          <div class="row-fluid" style="height: 100%">
            <div class="span8" style="height: 100%">
              <a href=<%=root_path(:locale => I18n.locale)%>><h1 class="heading"><%=t("site_title")%></h1></a>
            </div>
            <div class="span4" style="height: 100%">
              <%= link_to(image_tag("logo-en.png"), "http://www.transparency.ge", :target => "_blank", :title => t(".ti_link_title"), :class => "banner"	) %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="navigation-menu">
      <div class="container">
        <div class="row-fluid parent-block"> 
          <div class="span2">
            <%= link_to( t("Search Page"), root_path(:locale => I18n.locale),:class=>"nav-item") %>
          </div>
          <div class="span3">
            <%= link_to( t("Annual Data"), {:controller => '/analysis', :action => "index"}, :class=>"nav-item") %>
          </div>
          <div class="span2">
            <%= link_to( t("About"), {:controller => '/root', :action => "about"}, :class=>"nav-item") %>
          </div>
          <div class="span3 offset1">
            <% if user_signed_in? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle nav-item" data-toggle="dropdown" >
                    <%= current_user.email %>
                    <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li>
       		         <%= link_to t("Account"), user_path %>
				          </li>
                  <% if current_user.role == 'admin' %>
                    <li>
       		            <%= link_to t("Admin"), admin_path %>
				            </li>
                  <% end %>
                  <li>
       		         <%= link_to t('.sign_out'), destroy_user_session_path %>
				          </li>
                </ul>
              </li>
            <% else %>
               <%= link_to 'Sign In', new_user_session_path, :class=>"nav-item" %>
            <% end %>
          </div>

          <div class="span1">
	          <% I18n.available_locales.each do |locale| %>
		          <% if locale != I18n.locale %>
			          <%= link_to t("app.language.#{locale}"), params.merge(:locale => locale) %>
		          <% end %>
	          <% end %>
          </div>
        </div>
      </div>
    </div>

    <div id="content"><%= content_for?(:content) ? yield(:content) : yield %></div>

    <div id="push"></div>
  </div>
  <div id="footer">
    <div class="container">
      <%= link_to(image_tag("sida_logo.gif", :size => "100x100"), "http://www.sida.se/English/", :target => "_blank"	) %>
      <%= link_to(image_tag("slovak_aid_logo.jpg", :size => "100x100" ), "http://eng.slovakaid.sk/", :target => "_blank"	) %>
    </div>
   </div>

  <div id="dialog-confirm" title="Delete?">
  <p><span class="ui-icon ui-icon-alert" style="float: left; margin: 0 7px 20px 0;"></span>This item will be permanently deleted, Are you sure?</p>
  </body>

</html>
