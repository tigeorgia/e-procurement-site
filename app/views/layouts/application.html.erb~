<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    

    <%= stylesheet_link_tag "application", :media => "all" %>
    <!-- Javascripts
    ================================================== -->
    <%= javascript_include_tag "application" %>
    
		<script type="text/javascript">
			I18n.defaultLocale = "<%= I18n.default_locale %>";
			I18n.locale = "<%= I18n.locale %>";
		</script>

		<% if Rails.env.production? %>
			<!-- insert google analytics for production site here -->
		<% elsif Rails.env.staging? %>
			<!-- insert google analytics for staging site here -->
		<% end %>

    <title><%= content_for?(:title) ? "#{yield(:title)} > #{t('app.common.app_name')}" :
				t('app.common.app_name') %></title>
    <%= csrf_meta_tags %>
	  <%= include_gon %>


    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

		<!--[if IE 6]><%= stylesheet_link_tag 'ie6' %><![endif]-->
	  <!--[if IE 7]><%= stylesheet_link_tag 'ie7' %><![endif]-->

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag '/favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>

          <div class="container nav-collapse">
            <ul class="nav">
              <li><%= link_to("Search Page", root_path(:locale => I18n.locale),:class=>"brand font") %></li>
              <li><%= link_to("Corruption Finder", {:controller => :corruption_finder, :action => "index"},:class=>"brand font") %></li>
              <li><%= link_to("Analysis", {:controller => :analysis, :action => "index"},:class=>"brand font") %></li>
		          <% if user_signed_in? %>
				        <li class="dropdown">
				          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
				                <%= current_user.email %>
				                <b class="caret"></b>
				          </a>
				          <ul class="dropdown-menu">
						        <li>
				 		         <%= link_to "Account", user_path %>
										</li>
                    <% if current_user.role == 'admin' %>
                      <li>
				 		            <%= link_to "Admin", admin_path %>
										  </li>
                    <% end %>
						        <li>
				 		         <%= link_to t('.sign_out'), destroy_user_session_path %>
										</li>
				          </ul>
				        </li>
		          <% else %>
                <li class="nav" >
                  <%= link_to 'Sign In', user_path %>
                </li>
              <% end %>
            </ul>

	          <ul class="nav pull-right">
							<% I18n.available_locales.each do |locale| %>
								<% if locale != I18n.locale %>
									<li><%= link_to t("app.language.#{locale}"), params.merge(:locale => locale) %></li>
								<% end %>
							<% end %>
	          </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

		<div id="wrapper">
		  <div class="container">
		    <div class="content">
		      <div class="row">
		         <div class="span12">
							<% flash.each do |key, msg| %>
								<div class="alert <%= flash_translation(key) %> fade in">
									<a href="#" data-dismiss="alert" class="close">×</a>
									<%= msg %>
								</div>
							<% end %>

	            <%= yield %>
	          </div><!--/span-->
		      </div><!--/row-->
		    </div><!--/content-->
		   </div> <!-- /container -->
		</div><!--/wrapper-->

    <div class="push"><!--//--></div>

    <footer class="clear">
			<div id="left">
			  <ul>
			    <li id="ti_logo">
			      <%= link_to(image_tag("ti-logo.jpg"), "http://www.transparency.ge", :target => "_blank", :title => t(".ti_link_title")	) %>
			    </li>
			  </ul>
			</div>
		  <div id="middle">
				&nbsp;
			</div>
    </footer>


  </body>
</html>
