$(function () {
  $(".procurer_subscription").html("<%= escape_javascript( button_to t("Remove From Profile"), {:controller => :user, :action => "remove_procurer_watch", :procurer_watch_id => @procurer_watch_id, :user_id => current_user.id}, :remote => true, :class => "user-button") %>");
  return false;
});
