$(function () {
  $("<%= "#"+@buttonID %>").html("<%= escape_javascript( button_to "Subscribe", {:action => "subscribe_search", :search_id => @search.id, :buttonID => @buttonID}, :remote => true ) %>");
  return false;
});
