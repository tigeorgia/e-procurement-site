$(function () {
  $("<%= "#"+@buttonID %>").html("<%= escape_javascript( button_to t("Unsubscribe"), {:action => "unsubscribe_procurer", :procurer_watch_id => @procurer_watch_id, :buttonID => @buttonID}, :remote => true) %>");
  return false;
});
