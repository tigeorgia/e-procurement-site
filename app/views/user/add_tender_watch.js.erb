$(function () {
  $(".tender_subscription").html("<%= escape_javascript( button_to "Remove from Profile", {:controller => :user, :action => "remove_tender_watch", :tender_url => @tenderUrl}, :remote => true) %>");
  return false;
});
