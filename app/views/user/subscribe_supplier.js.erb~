$(function () {
  $("<%= "#"+@buttonID %>").html("<%= escape_javascript( button_to t("Unsubscribe"), {:action => "unsubscribe_supplier", :tender_watch_id => @tender_watch_id, :buttonID => @buttonID}, :remote => true) %>");
  return false;
});
